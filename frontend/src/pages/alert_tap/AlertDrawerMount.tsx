/*
import { NotificationDrawer } from "./NotificationDrawer";
import { useAlertDrawer } from "./AlertDrawerStore";

export default function AlertDrawerMount() {
  const { open, setOpen, items, unread } = useAlertDrawer();
  return (
    <NotificationDrawer
      items={items}
      unreadCount={unread}
      open={open}
      onOpenChange={setOpen}
    />
  );
}
*/